# Sesión Sentinel Omega — 2026-09-11 → 13

Zona: America/Mexico_City (CST, UTC-6).  
Ámbito principal: Prod /home/deamon/workspaces/sentinel_omega (más Dev/Test y watchdog).

## Resumen ejecutivo

1. **Telegram alerts caídas**: sentinel-omega y sentinel-omega-scheduler ambos corrían launcher.py → ciclo atascado en #1. Fix: deshabilitar scheduler, reiniciar omega, parchear 
etwork_watchdog para no reactivar el scheduler.
2. Dashboard API :8788 / UI :5174 apuntando a **DB Prod**.
3. Enriquecimiento de mensajes Telegram (nodo / lag).
4. Política Ollama: Windows ollama.exe (Startup Ollama.lnk) posee :11434; ollama.service en Kali debe permanecer **disabled** (enable → crash-loop).
5. Backup offbox: cron 30 3 * * * → /watchdog/scripts/offbox_backup.sh → /mnt/c/Users/elanz/watchdog-backups/; gap Sep 12 (WSL dormido); catch-up watchdog_2026-09-13_1253.tar.gz.
6. Pytest: Schumann espera None (sin fake 7.83); pytrends en workspaces/.venv; tests Alfa2 alineados a API canónica (**7 passed**). Agente-C / Agente-T.
7. Pendiente: firmas --entrenar opcional; gate de enable en prod.

## 1. Telegram / dual launcher (ciclo #1)

- Síntoma: alertas Telegram sin avanzar; ambos units sentinel-omega y sentinel-omega-scheduler ejecutaban launcher.py y quedaban en ciclo #1.
- Remediation:
  - Deshabilitar / stop sentinel-omega-scheduler.
  - Restart sentinel-omega (un solo launcher).
  - Parche en 
etwork_watchdog: no reiniciar el scheduler.
- Resultado esperado documentado: un solo proceso launcher activo; Telegram vuelve a fluir con el ciclo normal.

## 2. Dashboard → Prod DB

- API FastAPI en **:8788** y UI Vite en **:5174** configurados / verificados contra la DB de Prod (no inventar métricas de frescura aquí; ver health del día).

## 3. Telegram message enrichment

- Mensajes enriquecidos con contexto de **nodo** y **lag** (trazabilidad operativa en alertas).

## 4. Ollama — política Windows vs Kali

- Owner de :11434: Windows ollama.exe via Startup/Ollama.lnk.
- En Kali: ollama.service **must stay disabled**. Habilitarlo provoca crash-loop (conflicto de puerto / servicio).
- API Ollama responde **200** vía instancia Windows.

## 5. Offbox backup

- Cron: 30 3 * * * → /watchdog/scripts/offbox_backup.sh
- Destino: /mnt/c/Users/elanz/watchdog-backups/
- Gap: **2026-09-12** (WSL asleep; no corrida nocturna).
- Catch-up: watchdog_2026-09-13_1253.tar.gz generado/confirmado el 13.

## 6. Pytest / Agente-C / Agente-T

- Schumann: tests esperan None cuando no hay dato real (no fake **7.83**).
- pytrends instalado/disponible en workspaces/.venv.
- Alfa2: tests alineados a la API canónica — **7 passed**.
- Trabajo de estabilización atribuido a Agente-C / Agente-T en esta ventana.

## 7. Pendientes (sin inventar métricas)

- Opcional: firmas --entrenar.
- Gate de enable en prod (no habilitar entrenar/firmas/remapeo sin gate explícito).
- Scheduler permanece disabled a propósito (evitar dual launcher).

## Docs relacionados

- SYSTEM_HEALTH_2026-09-13.md
- Changelog: workspaces/sentinel_omega/CHANGELOG.md y /home/deamon/CHANGELOG.md
- Sesión previa: SESSION_2026-09-10.md / SYSTEM_HEALTH_2026-09-10.md
